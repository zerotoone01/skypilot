# A minimal example.
#
# Runs a task that simply lists the default conda environments.
#
# Usage:
#   sky launch -c min minimal.yaml
#   sky down -c min

resources:
  cloud: aws

setup: |
  # exit 1
  echo "running setup"

run: |
  conda env list
